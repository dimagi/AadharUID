language: android
script: "./gradlew check"
android:
  components:
    - android-22
    - build-tools-22.0.1
    - extra-android-m2repository
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
